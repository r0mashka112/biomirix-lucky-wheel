const path = require('path');

module.exports = {
    config: path.resolve(
        'src/config/database-cli.config.cjs'
    ),

    'models-path': path.resolve(
        'src/database/models'
    ),

    'migrations-path': path.resolve(
        'src/database/migrations'
    )
};